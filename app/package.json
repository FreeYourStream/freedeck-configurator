{
  "name": "freedeck-app",
  "version": "0.0.1",
  "description": "The FreeDeck App",
  "main": "main.js",
  "scripts": {
    "dev": "nodemon --watch ./src --exec 'npm run build && electron' ./dist/App.js",
    "start": "electron ./dist/App.js",
    "build": "tsc -p tsconfig.json"
  },
  "author": "Kilian Gosewisch",
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "electron": "^16.0.1",
    "electron-builder": "^22.14.5",
    "nodemon": "^2.0.15",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  }
}
