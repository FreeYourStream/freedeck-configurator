overwrite: true
schema: "../freedeck-backend/schema.gql"
documents: "./src/graphql/**/*.gql"
generates:
  src/generated/types-and-hooks.ts:
    config:
      avoidOptionals: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
